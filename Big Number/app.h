#include "bnum.h"
#include <fstream>

namespace app{
    void factorial(const bnum& number);
    /**
     * retorna um arquivo de tabela de frequencia de digitos
     * */
    void frequencetable(const bnum& number);
};